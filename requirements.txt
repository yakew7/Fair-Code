# Fair Code — Python dependencies
# https://github.com/yakew7/Fair-Code
#
# Install: pip install -r requirements.txt
# Python 3.8+ required

# ── Core data & ML ────────────────────────────────────────────────────────
numpy>=2.4.6           # array ops shared by every audit script and faircode/
pandas>=3.0.3          # DataFrames, CSV loading, crosstabs
scikit-learn>=1.9.0    # RandomForestClassifier, train/test split, metrics

# ── Statistical testing ───────────────────────────────────────────────────
scipy>=1.18.0          # chi2_contingency for proxy variable detection

# ── Fairness metrics ──────────────────────────────────────────────────────
fairlearn>=0.14.0      # ExponentiatedGradient / ThresholdOptimizer (benchmark harness S3/S4)

# ── Visualisation ─────────────────────────────────────────────────────────
matplotlib>=3.11.0     # Bias bar charts, fairness trade-off plots

# ── Benchmark harness (faircode benchmark / faircode/audit.yaml manifests) ─
pyyaml>=6.0            # parses audit.yaml manifests