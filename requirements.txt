# Fair Code — Python dependencies
# https://github.com/yakew7/Fair-Code
#
# Install: pip install -r requirements.txt
# Python 3.8+ required

# ── Core data & ML ────────────────────────────────────────────────────────
numpy>=2.4.6           # Numerical ops (pandas/sklearn dependency)
pandas>=3.0.3          # DataFrames, CSV loading, crosstabs
scikit-learn>=1.9.0    # RandomForestClassifier, train/test split, metrics

# ── Statistical testing ───────────────────────────────────────────────────
scipy>=1.17.1          # chi2_contingency for proxy variable detection

# ── Explainability ────────────────────────────────────────────────────────
shap>=0.52.0           # SHAP values — feature attribution & bias auditing

# ── Fairness metrics ──────────────────────────────────────────────────────
fairlearn>=0.13.0      # MetricFrame, equalized odds, demographic parity ratio

# ── Visualisation ─────────────────────────────────────────────────────────
matplotlib>=3.10.9     # Bias bar charts, fairness trade-off plots
